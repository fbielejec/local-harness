[package]
name = "rag-index"
version.workspace = true
edition.workspace = true

[dependencies]
anyhow = { workspace = true }
serde_json = { workspace = true }
rag-core = { path = "../core" }
rag-chunk = { path = "../chunk" }
rag-embed = { path = "../embed" }
# Deterministic (v5) point ids from the citation_id -> idempotent re-index.
uuid = { version = "1", features = ["v5"] }
# Qdrant gRPC client (async) — the live index target.
qdrant-client = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
