[package]
name = "ep-rag-parse"
version.workspace = true
edition.workspace = true

[[bin]]
name = "parse-gate"
path = "src/bin/gate.rs"

[dependencies]
anyhow = { workspace = true }
# Pure-Rust PDF text extraction (no native dep). The maximal-Rust first choice;
# we escalate to pdfium-render only if the gate shows its quality is inadequate.
pdf-extract = "0.12"
